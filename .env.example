# Root-level .env consumed by docker-compose.yml for variable substitution.
# Copy to `.env` and change SECRET_KEY before any real deployment.
SECRET_KEY=change_this_to_a_long_random_string_before_deploying
